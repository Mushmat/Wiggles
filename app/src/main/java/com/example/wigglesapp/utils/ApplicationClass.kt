package com.example.wigglesapp.utils

import android.app.Application

//Currently Empty. For Future Initialization

// Custom Application class for WigglesApp
class WigglesApp : Application() {
    // This method is called when the application is starting, before any activity, service, or receiver objects have been created.
    override fun onCreate() {
        super.onCreate()
        // Initialize any application-wide resources or configurations here
    }
}